# Model Assets

Place a static FBX at `assets/models/sample_static.fbx` to exercise the Assimp import path in the sample app.

The app falls back to a procedural textured cube when this file is not present.
